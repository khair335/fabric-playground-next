import React from 'react';

const PageTitle = ({ title }) => {
  return (
    <div className='font-unbounded text-6.5xl font-normal leading-[68px] text-black tracking-[0.5px]'>{title}</div>
  );
};

export default PageTitle;