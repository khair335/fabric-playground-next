import React from 'react';

const SectionMiniTitle = ({ title }) => {
  return (
    <div className='text-lg font-normal leading-5 text-secondary-8 tracking-[0.5px] font-unbounded '>{title}</div>
  );
};

export default SectionMiniTitle;