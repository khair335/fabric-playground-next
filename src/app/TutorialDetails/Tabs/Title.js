import React from 'react';

const Title = ({ title }) => {
  return (
    <h6 className='font-inter text-sm font-bold leading-[150%] text-secondary-6 uppercase'>
      {title}
    </h6>
  );
};

export default Title;